
`lms link disable` 命令在本设备上禁用 LM Link。该设备将不再连接到 Link 上的其他设备，也不再对它们可见。

## 禁用 LM Link

```shell
lms link disable
```

你可以随时用 [`lms link enable`](/docs/cli/link/link-enable) 重新启用 LM Link。

### 了解更多

关于 LM Link 的完整概览，参见 [LM Link 文档](/docs/lmlink)。
