
`lms link set-device-name` 命令为本设备设置一个显示名称，对 Link 上的其他设备可见。

## 重命名本设备

```shell
lms link set-device-name "My Mac Studio"
```

新名称会立即生效，并可通过 [`lms link status`](/docs/cli/link/link-status) 对已连接的对端可见。

### 了解更多

关于 LM Link 的完整概览，参见 [LM Link 文档](/docs/lmlink)。
